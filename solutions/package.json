{
  "name": "@Hyperlisk/aoc-solutions",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "compile": "tsc --module es2022 --target es2022 --moduleResolution node --rootDir ./ --outDir ./js",
    "solution": "pnpm run -r build && sh run-solution.sh",
    "test": "pnpm -r --filter '@Hyperlisk/aoc-lib' build && pnpm run build && NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@Hyperlisk/aoc-lib": "workspace:*",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2",
    "jest": "^29.3.1",
    "typescript": "^4.9.3"
  }
}
